let Koa,Router,mongoose,api,bodyParser;_7c5‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_7c5‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_7c5‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_7c5‍.w("./api",[["default",["api"],function(v){api=v}]]);_7c5‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);





require('dotenv').config();

const { PORT, MONGO_URL } = process.env;

mongoose
    .connect(MONGO_URL, { useNewUrlParser: true, useFindAndModify: false })
    .then(() => {
        console.log('Connected to MongoDB');
    })
    .catch((e) => {
        _7c5‍.g.console.log(e);
    });

// const api = require('./api');

const app = new Koa();
const router = new Router();

// router.get('/', (ctx) => {
//     ctx.body = 'ホーム';
// });
// router.get('/about/:name?', (ctx) => {
//     const { name } = ctx.params;
//     ctx.body = name ? `${name}の紹介` : '紹介';
// });

// router.get('/posts', (ctx) => {
//     const { id } = ctx.query;
//     ctx.body = id ? `ホスト#${id}` : 'ホストがありません。';
// });

router.use('/api', api.routes());

app.use(bodyParser());
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _7c5‍.g.console.log('Server Start!!at%d', port);
});
